{
    "version": "0.0.1",
    "configurations": [
        {
            "name": "Attach 9229 --inspect",
            "type": "node",
            "request": "attach",
            "protocol": "inspector",
            "port": 9229,
            "localRoot": "${workspaceRoot}",
            "remoteRoot": "/opt/node_app/app"
        }
    ]
}
